name: Azure Static Web Apps CI/CD - Phase 6 Analytics

on:
  push:
    branches:
      - phase-6-analytics-integration
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - phase-6-analytics-integration

# Concurrency control for Phase 6 deployments
concurrency:
  group: ci-${{ github.ref }}-phase-6
  cancel-in-progress: true

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job - Phase 6
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Clean install dependencies
        run: |
          npm ci --prefer-offline --no-audit

      - name: Build Application
        run: npm run build
        env:
          NODE_ENV: production
          VITE_PHASE: '6'
          VITE_PHASE_NAME: 'Analytics Integration'

      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_PORTFOLIO_V2_PHASE_6_ANALYTICS }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: 'upload'
          app_location: '/'
          output_location: 'dist'
          app_build_command: 'npm run build'

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job - Phase 6
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_PORTFOLIO_V2_PHASE_6_ANALYTICS }}
          action: 'close'
